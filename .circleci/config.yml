version: 2
jobs:
  build:
    working_directory: ~/onepage/frontend
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      # - restore_cache:
      #     keys:
      #       - dependency-cache-{{ checksum "package.json" }}
      #       - dependency-cache-
      #     path: .
      - run: yarn install
      # - save_cache:
      #     key: dependency-cache-{{ checksum "package.json" }}
      #     paths:
      #       - node_modules
      - run: yarn test
